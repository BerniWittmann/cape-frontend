/* ============
 * Mutation types for the injectionMapping module
 * ============
 *
 * The mutation types that are available
 * on the injectionMapping module.
 */
export const STORE = 'STORE'
export const UPDATE = 'UPDATE'

export default {
  STORE,
  UPDATE
}
